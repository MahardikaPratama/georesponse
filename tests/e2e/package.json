{
	"name": "georesponse-e2e",
	"version": "1.0.0",
	"private": true,
	"description": "End-to-end golden-path test for GeoResponse (Playwright), run against a running stack (./run.sh).",
	"scripts": {
		"test": "playwright test",
		"test:headed": "playwright test --headed",
		"install-browsers": "playwright install --with-deps chromium"
	},
	"devDependencies": {
		"@playwright/test": "^1.48.0",
		"@types/node": "^22.9.0"
	}
}
