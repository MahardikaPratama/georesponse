import type { DatasetSize, MapLibraryId, ScenarioId } from '../types/benchmark.types';

export const DEFAULT_LIBRARY: MapLibraryId = 'leaflet';
export const DEFAULT_SCENARIO: ScenarioId = 'S01';
export const DEFAULT_DATASET_SIZE: DatasetSize = 'medium';
